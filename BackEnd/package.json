{
  "name": "backend",
  "version": "1.0.0",
  "description": "AF final project backend using nodejs and mongodb",
  "main": "index.js",
  "scripts": {
    "build": "cd FrontEnd && npm run build",
    "install-client": "cd FrontEnd && npm install && npm start",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node server.js",
    "FrontEnd": "cd FrontEnd && npm start",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.15"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "mocha": "^9.0.1",
    "should": "^13.2.3",
    "supertest": "^6.1.3"
  }
}
